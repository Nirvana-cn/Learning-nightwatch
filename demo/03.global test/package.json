{
  "name": "",
  "version": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rd /s/q output && nightwatch --env another"
  },
  "devDependencies": {
    "chromedriver": "^2.46.0",
    "nightwatch": "^1.0.18"
  }
}
